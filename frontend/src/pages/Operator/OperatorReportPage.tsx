import React from 'react';
import ReportView from '../../components/faults/ReportView';

/** Operator Report tab — alarm counts grouped by severity or status. */
export default function OperatorReportPage() {
  return <ReportView />;
}
