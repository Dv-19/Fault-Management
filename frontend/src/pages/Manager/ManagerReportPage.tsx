import React from 'react';
import ReportView from '../../components/faults/ReportView';

/** Manager Report tab — alarm counts grouped by severity or status. */
export default function ManagerReportPage() {
  return <ReportView />;
}
