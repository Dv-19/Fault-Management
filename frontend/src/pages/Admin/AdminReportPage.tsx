import React from 'react';
import ReportView from '../../components/faults/ReportView';

/** Admin Report tab (US07) — alarm counts grouped by severity or status. */
export default function AdminReportPage() {
  return <ReportView />;
}
